// 也可以在根目录下新建一个.env文件夹，写在.env文件中
const SERVER_POST = 3000;

export default SERVER_POST;

// import dotenv from 'dotenv';

// dotenv.config();

// console.log(process.env);

// export default {
//     SERVER_POST: process.env.SERVER_POST,
// };
